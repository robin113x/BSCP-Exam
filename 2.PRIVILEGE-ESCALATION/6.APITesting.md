# API Testing


### API Reverse Engineering 

sudo mitmproxy2swagger -i flows -o spec.yaml -p http://crapi.local -f flow

sudo mitmproxy2swagger -i flows -o spec.yaml -p http://crapi.local -f flow --examples


# API Recon

https://reqres.in
APIMATIC

# Burp Extension
	- OpenAPI Parser



###

Payload
```
	- Log in with wiener:peter and update the email.
	- In Proxy > HTTP history, send the PATCH /api/user/wiener request to Repeater.
	- Sending it retrieves wiener’s credentials.
	- Modify the path:
	- /api/user → returns error (missing identifier).
	- /api → returns API documentation.
	- Open the documentation in Burp’s browser (interactive).
	- Use the DELETE endpoint with carlos to remove the user and solve the lab.
```